# Render Blueprint — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: razor-backend
    runtime: node
    buildCommand: npm install && npm run build && npx prisma migrate deploy --schema=prisma/schema.prisma
    startCommand: npm start
    healthCheckPath: /
