# Render Blueprint — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: razor-backend
    runtime: node
    buildCommand: npm install && npm run build && npm run db:deploy:render
    startCommand: npm start
    healthCheckPath: /
